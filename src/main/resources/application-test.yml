spring:

  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:public;
      username: sa
      password:
      settings.web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    defer-datasource-initialization: true # ddl-auto 실행후 data.sql 실행되도록
  sql:
    init:
      mode: always
      continue-on-error: false
      data-locations: classpath:data.sql

jwt:
  secret: agoraSecretKey112312341251413542364563456345342543